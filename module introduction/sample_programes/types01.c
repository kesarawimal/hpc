#include <stdlib.h>
#include <stdio.h>

//printf function will print the assigned integer values as denoted by %d and performs the mathematical function (x/y)
int main() {
  int x = 10;
  int y = 3;

  printf("%d / %d = %d\n", x, y, x/y);
  return EXIT_SUCCESS;
}
